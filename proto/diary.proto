syntax = "proto3";

option go_package = "cloudrun-grpc/go-contact/proto/contactpb";

package contact;

service Interpret {
    rpc Interpret (Diary) returns (DiaryReply) {}
}

message Diary {
    string diary = 1;
}

message DiaryReply {
    string name = 1;
}
